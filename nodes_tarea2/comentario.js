const config = require("./dbconfig")

class Comentario{
    constructor(nombre,estado,fecha,comentario){
        this.nombre=nombre;
        this.estado=estado;
        this.fecha = fecha;
        this.comentario=sexo;
    }
}

module.exports = Comentario;